[package]
name = "knights-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "knights_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
base64 = "0.22"
